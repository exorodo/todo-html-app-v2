## Technical Approach
- let todos = []; addTodo() validates, pushes, renders, clears input
- renderTodos() clears innerHTML, loops todos, creates li elements
- Event listeners: input keydown Enter, button click
