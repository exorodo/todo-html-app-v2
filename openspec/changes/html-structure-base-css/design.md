## Technical Approach
Single-file: everything in `index.html` with inline `<style>` and `<script>`.
CSS custom properties for theming. System font stack. Mobile-first responsive.
DOM: `.app` > h1 + #input-area + #todo-list + #filters.
