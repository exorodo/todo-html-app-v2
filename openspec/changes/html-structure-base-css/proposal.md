## Why
Foundation for a single-file HTML Todo app. All subsequent features build on this skeleton.

## What Changes
- Create `index.html` with HTML5 doctype, charset, viewport meta
- CSS reset, custom properties for theming, system font stack
- Centered `.app` container with header and placeholder sections

## Capabilities
### New Capabilities
- `html-skeleton`: Base HTML5 document with responsive layout and CSS foundation
### Modified Capabilities
## Impact
- Creates `index.html` — the single file all other tickets modify
